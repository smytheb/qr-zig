# Zig build artifacts
.zig-cache/
zig-out/

# Release artifacts (cross-compiled tarballs + checksums)
dist/

# Editor / OS noise
*.swp
.DS_Store

# Local test scratch (rasterized QR images from the cross-decoder check)
/scratch/
*.png
